version: "3.8"
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: recrm
      POSTGRES_PASSWORD: recrm_password
      POSTGRES_DB: real_estate_crm
    ports:
      - "5432:5432"
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
